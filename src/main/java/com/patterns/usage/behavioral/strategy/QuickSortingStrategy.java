package com.patterns.usage.behavioral.strategy;

import com.patterns.utils.PatternHelper;

public class QuickSortingStrategy implements SortingStrategy {

    @Override
    public int[] sort(int[] array) {
        PatternHelper.print("Sort Using Quick Sorting Strategy");
        //do smth
        return new int[0];
    }
}
